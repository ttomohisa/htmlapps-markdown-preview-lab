# Dependencies

Runtime dependencies: **none**.

No CDN, remote font, external Markdown parser, analytics library, or network API is required. See `dependencies.json` for the machine-readable manifest.
